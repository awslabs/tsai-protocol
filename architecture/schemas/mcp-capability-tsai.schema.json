{
  "$comment": "Copyright Amazon.com Inc. or its affiliates. SPDX-License-Identifier: Apache-2.0",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tsaiprotocol.org/schemas/mcp-capability-tsai.json",
  "title": "MCP TSAI Capability Declaration",
  "description": "TSAI capability declaration in an MCP initialization response",
  "type": "object",
  "required": ["required", "trustedAuthorities"],
  "properties": {
    "required": {
      "type": "boolean",
      "description": "Whether TSAI credentials are mandatory"
    },
    "trustedAuthorities": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "format": "uri",
        "pattern": "^https://",
        "description": "Accepted Trust Authority HTTPS issuer identifier"
      },
      "description": "List of accepted Trust Authority issuers"
    },
    "aud": {
      "type": "string",
      "description": "The audience identifier an agent places in the key-binding JWT aud on a transport with no HTTP origin (MCP over stdio). Stable across the server's sessions. Omitted where the HTTPS origin is the audience."
    }
  },
  "additionalProperties": false
}
