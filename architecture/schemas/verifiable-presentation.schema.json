{
  "$comment": "Copyright Amazon.com Inc. or its affiliates. SPDX-License-Identifier: Apache-2.0",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tsai.example.org/schemas/verifiable-presentation.json",
  "title": "TSAI VP-JWT Payload",
  "description": "JWT Claims Set for TSAI Verifiable Presentations (VP-JWT encoding per W3C VC-JOSE-COSE). The JWS payload IS the Verifiable Presentation directly: the VP properties (@context, type, verifiableCredential) are top-level claims and there is no `vp` wrapper (VC-JOSE-COSE prohibits the `vc`/`vp` claim names). TSAI 1.0 VP-JWTs MUST NOT include claims beyond those defined in this schema. Future versions may relax this constraint.",
  "type": "object",
  "required": [
    "@context",
    "type",
    "verifiableCredential",
    "iss",
    "aud",
    "iat",
    "exp"
  ],
  "properties": {
    "@context": {
      "type": "array",
      "contains": {
        "const": "https://www.w3.org/ns/credentials/v2"
      }
    },
    "type": {
      "type": "array",
      "contains": {
        "const": "VerifiablePresentation"
      }
    },
    "verifiableCredential": {
      "type": "array",
      "minItems": 1,
      "maxItems": 1,
      "items": {
        "type": "string",
        "description": "VC-JWT encoded TSAI credential"
      },
      "description": "TSAI 1.0 requires exactly one credential per VP"
    },
    "iss": {
      "type": "string",
      "pattern": "^did:",
      "description": "Agent DID (must match credentialSubject.id in enclosed VC)"
    },
    "aud": {
      "type": "string",
      "description": "Service Provider identifier. HTTP: request URL origin. MCP stdio: server identifier. Push notifications: webhook URL origin."
    },
    "iat": {
      "type": "integer",
      "description": "Issued-at (Unix timestamp). Service Providers MUST validate within ±30 seconds of current time."
    },
    "exp": {
      "type": "integer",
      "description": "Expiration (Unix timestamp). MUST be no more than 60 seconds after iat."
    },
    "nonce": {
      "type": "string",
      "description": "Nonce provided by the Service Provider. OPTIONAL for T0/T1, REQUIRED for T2/T3. Eliminates replay window when present."
    }
  },
  "additionalProperties": false,
  "$defs": {
    "_extensibility_note": {
      "description": "additionalProperties is false for TSAI 1.0 strictness. Future versions may relax this to allow extension claims."
    }
  }
}
